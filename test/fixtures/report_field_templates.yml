# Read about fixtures at http://ar.rubyonrails.org/classes/Fixtures.html

one:
  name: one
  applicable_on: "[String]"
  function: "objs.collect { |o| [o, o.size] }"
  render_options: "{:method=>:matrix}"

two:
  name: two
  applicable_on: "[String]"
  function: "objs.collect { |o| [o, o.uppercase] }"
  render_options: "{:method=>:matrix}"

for_odat_diagnoses:
  name: for_odat_diagnoses
  applicable_on: "[OdatDiagnosis]"
  function: "objs.collect { |o| [o.id, o.medical_record.full_name] }"
  render_options: "{:method=>:matrix}"
