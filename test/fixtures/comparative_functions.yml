# Read about fixtures at http://ar.rubyonrails.org/classes/Fixtures.html

one:
  name: one
  applicable_on: "[String]"
  function: "objs.collect { |o| o, o.size }"
  render_method: matrix
  render_options: NULL

two:
  name: two
  applicable_on: "[String]"
  function: "objs.collect { |o| o, o.uppercase }"
  render_method: matrix
  render_options: NULL
